package com.auction.server.dao;
import com.auction.shared.model.entity.*;
import java.util.*;
/** DAO cho User – đọc/ghi bằng Serialization. */
public class UserDAO {
    private static final String DATA_FILE = "data/users.dat";
    // TODO: save(User) / findById(String) / findByUsername(String) / findAll() / delete(String)
    // TODO: dùng ObjectOutputStream / ObjectInputStream
}
